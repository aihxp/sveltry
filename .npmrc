# Publish the @aihxp scope to GitHub Packages.
# The token is injected as NODE_AUTH_TOKEN in CI (see .github/workflows/release.yml).
@aihxp:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${NODE_AUTH_TOKEN}
always-auth=true
