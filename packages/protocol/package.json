{
  "name": "@sveltry/protocol",
  "version": "0.9.3",
  "private": true,
  "type": "module",
  "description": "Sentry-compatible wire protocol: DSN parsing, byte-accurate envelope parsing, auth extraction, event normalization, and issue fingerprinting.",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "test": "bun test",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "@sveltry/types": "workspace:*",
    "fflate": "^0.8.3"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "catalog:"
  }
}
